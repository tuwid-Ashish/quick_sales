const EMAIL = import.meta.env.VITE_APP_EMAIL || 'EMAIL';
const PHONE = import.meta.env.VITE_APP_PHONE || 'PHONE';
const address = import.meta.env.VITE_APP_ADDRESS || 'ADDRESS';

export const CONTACT = {   
    EMAIL,
    address,
    PHONE
};